# Benchmark plan

## Required model benchmarks

After adding the real dataset, record:

- Dataset row count before cleaning
- Dataset row count after cleaning
- Train/test split size
- Linear Regression MAE, RMSE, R2
- Random Forest MAE, RMSE, R2
- Best model name
- Top 10 important features

## Required dashboard benchmarks

Record:

- Dashboard startup time
- Model loading time
- Prediction response time
- Screenshot evidence of prediction output

## Current benchmark status

Real benchmark results are missing because the real AQI dataset has not been added yet.

